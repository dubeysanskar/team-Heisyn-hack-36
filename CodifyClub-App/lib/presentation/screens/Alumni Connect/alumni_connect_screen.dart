import 'package:codifyclub/Core/Constants/colors.dart';
import 'package:flutter/material.dart';

class AlumniConnectScreen extends StatelessWidget {
  const AlumniConnectScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.seaBlue,
    );
  }
}
